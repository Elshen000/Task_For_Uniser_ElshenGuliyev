@model List<Category>
@foreach (Category item in Model)
{
    <tr>
        <td>
            @item.Name
        </td>

        @if (item.Products.Count() > 0)
        {
            <td>
                @item.Products.Count()
            </td>
        }
        else
        {
            <td>
                Məhsul yoxdur
            </td>
        }
        <td>
            <a asp-controller="Categories" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger rounded-pill">Delete </a>

        </td>

    </tr>
}